# HTML Inbox

```sh
html-inbox publish ./report.html --title "SvelteKit Migration Report" --type report
```

Publishes a self-contained HTML file into the local private document library, starts or reuses the localhost viewer, and prints the local viewer URL.
