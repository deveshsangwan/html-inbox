{
  "name": "@html-inbox/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared contracts and validation for HTML Inbox.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm run build && pnpm run test:self",
    "test:self": "node dist/self-check.js"
  }
}
